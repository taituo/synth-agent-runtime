{
  "name": "synth-agent-runtime-temporal",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test test/*.test.ts",
    "live:interceptors": "tsx interceptors-live.ts",
    "live:driver": "tsx event-driver.ts",
    "live:swarm": "tsx swarm-driver.ts",
    "live:swarm-inference": "tsx swarm-inference-driver.ts",
    "live:park": "tsx park-live.ts",
    "live:corpus": "tsx corpus-inference-driver.ts",
    "live:mailbox-property": "tsx mailbox-property-driver.ts",
    "live:replay": "tsx replay-determinism.ts",
    "live:phases": "tsx phase-signals-driver.ts",
    "live:restart": "tsx restart-worker.ts",
    "live:flaky-gateway": "tsx flaky-gateway.ts"
  },
  "dependencies": {
    "@temporalio/activity": "^1.13.0",
    "@temporalio/client": "^1.13.0",
    "@temporalio/worker": "^1.13.0",
    "@temporalio/workflow": "^1.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
